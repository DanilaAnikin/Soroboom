<template>
  <div class="w-full flex justify-start">
    <div class="w-8/12 shadow-sm shadow-slate-200 py-4 px-12 flex justify-between rounded-3xl bg-white font-montserrat">
      <span>{{ $t('locale.first.navigation.timetable') }}</span>
      <span>{{ $t('locale.first.navigation.what_is_soroboom') }}?</span>
      <span>{{ $t('locale.first.navigation.blog') }}</span>
      <span>{{ $t('locale.first.navigation.gallery') }}</span>
    </div>
  </div>
</template>