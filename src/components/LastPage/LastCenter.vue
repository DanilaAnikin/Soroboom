<script setup lang="ts">

</script>

<template>
  <div class="flex flex-col sm:w-1/3 w-full sm:gap-5 gap-3">
    <span class="text-xl font-semibold">{{ $t('locale.last.center.menu') }}</span>
    <span class="text-lg">{{ $t('locale.first.navigation.timetable') }}</span>
    <span class="text-lg">{{ $t('locale.first.navigation.what_is_soroboom') }}?</span>
    <span class="text-lg">{{ $t('locale.first.navigation.blog') }}</span>
    <span class="text-lg">{{ $t('locale.first.navigation.gallery') }}</span>
    <span class="text-lg">{{ $t('locale.last.center.franchise') }}</span>
  </div>
</template>